{
  "name": "semcom-freight-estimator",
  "private": true,
  "version": "0.1.0",
  "description": "Internal freight estimating & container loading tool for Semcom",
  "workspaces": ["engine", "server", "web"],
  "scripts": {
    "build:engine": "npm run build -w @semcom/engine",
    "test": "npm run test -w @semcom/engine",
    "dev": "npm run build:engine && concurrently -n api,web -c blue,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev -w @semcom/server",
    "dev:web": "npm run dev -w @semcom/web",
    "build": "npm run build:engine && npm run build -w @semcom/web && npm run build -w @semcom/server",
    "start": "npm run start -w @semcom/server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  },
  "engines": { "node": ">=20" }
}
